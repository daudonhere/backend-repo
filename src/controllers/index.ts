import { login } from "./authController";
import { createUser, fetchAllUsers, fetchUserData, updateUserData } from "./userController";

export { login, createUser, fetchAllUsers, fetchUserData, updateUserData };